#pragma once

/**
 * Tests if the Bloom Filter constructor is working correctly.
 */
void test_bloom_filter_constructor();

/**
 * Tests if the Bloom Filter can correctly insert and query elements.
 */
void test_bloom_filter_insert_and_query();

/**
 * Tests if the Bloom Filter can be cleared correctly.
 */
void test_bloom_filter_clear();
