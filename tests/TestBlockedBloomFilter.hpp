#pragma once

/**
 * Tests if the Blocked Bloom Filter constructor is working correctly.
 */
void test_blocked_bloom_filter_constructor();

/**
 * Tests if the Blocked Bloom Filter can correctly insert and query elements.
 */
void test_blocked_bloom_filter_insert_and_query();

/**
 * Tests if the Blocked Bloom Filter can be cleared correctly.
 */
void test_blocked_bloom_filter_clear();